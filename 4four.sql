--let us talk about functions -> they take input parameter and they return an output
-- we can have both builtin as well as the user defined functions

-- let us llook at some string functions -> they take input string perform some operations and return output string

-- * UPPPER-> it returns uppercase string 
-- *LOWER -> it returns lowercase string
-- *LENGTH -> returns the length of the in put string
-- *SUBSTRING -> it extract substring from the string
-- *NOW -> returns the current system date and time
-- *CONCAT -> apas me strings ko merge karta hai 
-- *REPLACE -> string ke part ko replace karne hetu
-- *TRIM -> it helps in the removal of trailign and leading spaces

-- *now let us look at the most imp aggregate functions

-- *COUNT() returns number of values or rows in a table
-- *SUM() returns sum of all values
-- *AVG() returns average value
-- *MAX() returns maximum value
-- *MIN() returns minimum value
-- *ROUND() rounds a number to a specified places of decimal